# 📝 Member A: Daily Task List - Dec 27, 2025

## 🔍 Gap Analysis: What is Missing?

| Missing Component | Why it matters? | Risk Level |
| --- | --- | --- |
| **1. The "Blank Screen" Problem** | On Dec 26, you built an editor. But when the Admin logs in, they see a blank black box. **They don't know the syntax.** They don't know they need to type `WORKFLOW ...`. | 🔴 **Critical** |
| **2. Professional Feedback** | Currently, the React code uses `alert("Error")`. This looks unprofessional and disruptive. You need non-blocking "Toast" notifications. | 🟡 Medium |
| **3. Code Cleanup** | The project still contains `mock_compiler.py` and potentially hardcoded "Mock Mode" flags. These need to be purged to ensure the "Real" compiler is the only path. | 🟡 Medium |
| **4. End-to-End Verification** | Member A built the Frontend. Member B built the Bot. **Nobody has formally tested the connection.** You need a "Golden Path" test script. | 🔴 **Critical** |

---

## **Core Objective**

Here is the generated task to fill these gaps and finish the project.
Transition the project from "Development Mode" to "Demo Ready". Build the **Syntax Helper (Cheatsheet)** so admins know how to use the language, improve the UX with Toast notifications, and perform the final integration test with Member B.

## 🛠️ Tasks for Today

### 1. The Syntax Cheatsheet (Solves "Blank Screen" Problem)

* **Context:** Users cannot memorize your `.g4` grammar. They need copy-paste examples.
* **Task:** Create a `DocumentationPage.jsx` .
* **Content:**
* `WORKFLOW structure`: `WORKFLOW name ON trigger { ... }`
* `ACTION examples`: `ACTION: SEND_MESSAGE ...`, `ACTION: BAN_USER`
* `LOGIC examples`: `IF user.strikes > 3 { ... }`

* **Functionality:** Clicking a code snippet should copy it to the clipboard or insert it into the editor.

### 2. UX Upgrade (Toast Notifications)

* **Context:** Browser alerts stop the user workflow.
* **Task:** Install `react-hot-toast` (or similar).
* **Implementation:**
* **Success:** Show a green pop-up: "✅ Workflow 'anti_spam' deployed successfully!"
* **Error:** Show a red pop-up: "❌ Syntax Error on Line 3: Missing '}'"

* **Loading:** Disable the "Deploy" button and show a spinner text while waiting for the API.

### 3. Final Integration: The "Golden Path" Test

* **Context:** Verify the entire loop (Frontend -> API -> DB -> Bot).
* **Task:** Execute this exact sequence with Member B:

1. **Type** this script in your new Editor:

    ```text
    WORKFLOW final_check ON message {
        ACTION: REPLY_MESSAGE content="System Operational"
    }

    ```

2. **Click** Deploy. Verify the Green Toast appears.
3. **Check DB:** Confirm a new row exists in the `workflows` table.
4. **Trigger:** Have Member B type "test" in Discord.
5. **Verify:** The bot *must* reply "System Operational".

### 4. Cleanup

* [ ] Delete `src/compiler/mock_compiler.py`.
* [ ] Remove any `print("DEBUG: ...")` statements from `compiler.py`.
* [ ] Add a README file explaining how to start the Frontend (`npm start`) and Backend (`uvicorn ...`).

---

## 💻 Solution Snippet: The Cheatsheet Component

Add documentation page to the Website:

* The user must be login to see that documentation page
* Using the g4 documentation to as the reference

**Instruction:** Integrate this `Cheatsheet` component into your main `WorkflowEditor.jsx` page layout (e.g., using a Flexbox container to put the Editor on the left and Cheatsheet on the right).
